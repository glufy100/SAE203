from django.apps import AppConfig


class DriveConfig(AppConfig):
    # Configuration de l'application Drive dans le projet Django.
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'drive'
